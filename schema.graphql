type User @entity {
  id: ID! # address
  pods: [PodUser!] @derivedFrom(field: "user")
}

type Pod @entity {
  id: ID! # Pod ID
  users: [PodUser]! @derivedFrom(field: "pod")
}

type PodUser @entity {
  id: ID! # Set to `${user.id}-${pod.id}
  user: User!
  pod: Pod!
}
